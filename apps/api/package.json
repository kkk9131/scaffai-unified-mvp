{
  "name": "api",
  "version": "1.0.0",
  "description": "ScaffAI FastAPI Server",
  "scripts": {
    "setup": "pip install -r requirements.txt",
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install-deps": "pip install fastapi uvicorn[standard] pydantic python-multipart"
  }
}
